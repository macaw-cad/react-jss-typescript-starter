id: scaffold-page
fields:
  pageTitle: Example scaffolds for Sitecore JSS
placeholders:
  jss-main:
  - componentName: GridSection
    placeholders:
      jss-grid-section:
        - componentName: Hero
          fields:
            image:
              src: /data/media/img/umbrella.jpg
              alt: Umbrella for Sitecore JSS
        - componentName: GridColumns
          fields:
            columns: 1,10,1
          placeholders:
            jss-grid-column-2:
              - componentName: MarkdownViewer
                fields:
                  body: |
                    # Scaffolding new JSS components
                    With `npm run plop` new JSS components can be scaffolded.
                    On this page some sample scaffolded components are shown generated with
                    the following commands:
                    - `npm run plop jssClassComponents ScaffoldOfJssClassComponent`
                    - `npm run plop jssFunctionComponents ScaffoldOfJssFunctionComponent`
                    <hr/>
              - componentName: ScaffoldOfJssClassComponent
                fields:
                  heading: An example of a scaffolded JSS class component
              - componentName: ScaffoldOfJssFunctionComponent
                fields:
                  heading: An example of a scaffolded JSS function component
